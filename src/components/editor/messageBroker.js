export const handleMessage = (message) => {
    switch(message.type) {
        case "CONNACK":
            
        break;
        case "CONACC":

        break;
        case "FILE":

        break;
        case "CONDEC":

        break;
    }
}